// Importing necessary dependencies
import React from 'react';
import PostList from './components/PostList';

// Define the main App component
function App() {
  // Returning the JSX for App component
  return (
    <div className="App">
      <PostList />
    </div>
  );
}

// Exporting the App component
export default App;